# API base URL used by the frontend to communicate with the backend
# Uses NEXT_PUBLIC_ prefix so it is available in browser code (public, not secret)
# For local development this typically points to your local API instance
NEXT_PUBLIC_API_URL=